# Environment Configuration
# Copy this file to .env for local development

# Server Configuration
NODE_ENV=development
PORT=3001

# Authentication Credentials
# IMPORTANT: Change these in production!
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme
SESSION_SECRET=your-secret-key-here-change-in-production

# Frontend API Configuration (optional)
# Only set this if frontend and backend are deployed separately
# When both are served from the same Railway service, leave unset (uses relative /api path)
# Example for separate deployments: VITE_API_URL=https://your-backend.up.railway.app/api
# VITE_API_URL=

# Railway-specific (automatically set by Railway)
# PORT=8080 (Railway sets this automatically)
# RAILWAY_VOLUME_MOUNT_PATH=/data
